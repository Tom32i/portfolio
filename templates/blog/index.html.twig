{% extends 'base.html.twig' %}

{% block body %}
<ul>
    {% for article in articles %}
        <li>
            <a href="{{ path('blog_article', { slug: article.slug }) }}">
                {{ article.title }}
            </a>
        </li>
    {% endfor %}
</ul>
{% endblock %}
