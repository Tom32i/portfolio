{% extends 'talk/base.html.twig' %}

{% block content %}
    <p>Quelques exemples de conférences que je redonnerais avec plaisir :</p>
    {% for talk in talks %}
        <div class="talk">
            <p class="links">
                <span class="format">{{ talk.duration }}'</span>
                <span class="date">{{ talk.date|date('Y') }}</span>
                <a class="slides" target="_blank" href="{{ talk.slides }}">slides</a>
                {% if talk.video %}
                    <a class="video" target="_blank" href="{{ talk.video }}">vidéo</a>
                {% endif %}
            </p>
            <h3 class="title">{{ talk.title }}</h3>
            <p class="description">{{ talk.description }}</p>
        </div>
    {% endfor %}
    <hr>
    <div class="center">
        <p>Vous cherchez des conférenciers pour votre évènement ? 👀</p>
        <p>👉 Mon <a href="mailto:thomas.jarrand@gmail.com">mail</a> et mon <a href="https://twitter.com/tom32i">Twitter</a> 👋</p>
    </div>
{% endblock %}
